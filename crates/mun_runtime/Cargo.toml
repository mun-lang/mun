[package]
name = "mun_runtime"
version = "0.2.0"
authors = ["The Mun Team <team@mun-lang.org>"]
edition = "2018"
homepage = "https://mun-lang.org"
repository = "https://github.com/mun-lang/mun"
license = "MIT OR Apache-2.0"
description = "A runtime to enable running Mun code from Rust"

[dependencies]
failure = "0.1.5"
libloading = "0.5"
mun_abi = { path = "../mun_abi" }
notify = "4.0.12"
parking_lot = "0.9"
tempfile = "3"

[dev-dependencies]
mun_compiler = { path="../mun_compiler" }
